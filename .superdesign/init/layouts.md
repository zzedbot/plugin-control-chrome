# Layouts

This MV3 extension has no shared application-shell component. The popup and options page are standalone documents. The controlled-tab experience is a viewport-fixed overlay injected by `extension/virtual-cursor.js`; its complete source is captured in `components.md`.
